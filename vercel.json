{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "headers": {
        "x-vercel-deployment-protection": "off"
      }
    }
  ]
}
